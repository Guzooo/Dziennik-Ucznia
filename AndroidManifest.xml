<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.Guzooo.DziennikUcznia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".EditActivity"
            android:label="@string/add_subject" />
        <activity
            android:name=".SettingActivity"
            android:label="@string/app_G"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".NoteActivity"
            android:windowSoftInputMode="stateHidden | adjustResize" />
        <activity
            android:name=".LessonPlanActivity"
            android:label="@string/lesson_plan" />
        <activity
            android:name=".LessonPlanEditActivity"
            android:label="@string/lesson_plan_add" />
        <activity
            android:name=".StatisticsActivity"
            android:label="@string/statistics" />
        <activity android:name=".DetailsAndEditActivity"></activity>
    </application>

</manifest>